<div class="toukousya">投稿者:<%= tweet.user.nickname %>, 投稿者の棋力:<%= tweet.user.kiryoku.name %>, 局面:<%= tweet.tag %></div>
  <div>
  <div class="index-img">
    <%= image_tag tweet.image %>
  </div>
  <%= link_to tweet_path(tweet.id), class: "detail", method: :get do %>
    <div class="detail-post"><%= "詳細を見る" %>
    </div> 
    <% end %>
</div>